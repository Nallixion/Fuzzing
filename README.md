# Fuzzing
Fuzz dotnet projects



## winsharpfuzz

[winsharpfuzz](https://github.com/nathaniel-bennett/winsharpfuzz)

```
> dotnet tool install Nallixion.WinSharpFuzz.CommandLine --version 0.1.0-alpha.1
You can invoke the tool from this directory using the following commands: 'dotnet tool run winsharpfuzz' or 'dotnet winsharpfuzz'.
Tool 'winsharpfuzz.commandline' (version 'o.1.0') was successfully installed. Entry is added to the manifest file C:\Source\Fuzzing\.config\dotnet-tools.json.

> dotnet tool install Nallixion.WinSharpFuzz.Instrument --version 0.1.0-alpha.1 
You can invoke the tool from this directory using the following commands: 'dotnet tool run winsharpfuzz-instrument' or 'dotnet winsharpfuzz-instrument'.
Tool 'winsharpfuzz.instrument' (version '0.1.0') was successfully installed. Entry is added to the manifest file C:\Source\Fuzzing\.config\dotnet-tools.json.
```